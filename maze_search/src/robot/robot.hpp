#pragma once
#include <vector>


class Robot {
    public:
        Robot(int initialX, int initialY);
        int setup();
        std::vector<int> state; // 1D, 2 values for (X, Y)

    private:

};
 